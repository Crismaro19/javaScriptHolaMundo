let a = 1;

function suma(n){
    n++;
}

suma(a);
console.log(a);


// los tipos de datos primitivos se copian cuando se asigna una variable

/**
 * los tipos de datos de referencia se referencian (se crea una referencia logica que apunta al objeto principal)
 * objetos
 * array
 * funciones
 */
